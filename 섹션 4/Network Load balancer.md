

```

1) TCP, UDP, TLS 요청을 로드밸런싱 해야 하는 경우에 사용
2) 리스너 프로토콜은 TCP, TCP_UDP, UDP, TLS 사용할 수 있음
3) 초당 수백만 개의 요청과 갑작스럽고 변동성이 높은 트래픽 패턴을 처리하도록 설계
4) 고도의 성능이 요구되거나 대기 시간이 낮아야 하는 애플리케이션에 적합
5) 게임 등의 수백만의 동시 사용자 처리에 적합 
6) 고정 IP 주소 할당 가능
7) 클라이언트 IP 주소 전달 가능
8) ALB처럼 리스너 규칙 설정 없음
9) 데이터 전송 보안을 위한 TLS 프로토콜 사용시 SSL/TLS 인증서를 배포해야 함
10) 인증서는 ACM 사용 또는 클라이언트 인증서 사용 가능







```
